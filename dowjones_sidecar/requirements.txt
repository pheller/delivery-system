fastapi==0.115.5
uvicorn==0.32.1
# yfinance and curl_cffi are gone with the live feed. The fixture needs
# nothing but the stdlib, so the image no longer carries a quote library or
# its HTTP client - and no longer inherits their breakage.
